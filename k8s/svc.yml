apiVersion: v1
kind: Service
metadata:
  name: weblate
  namespace: weblate-prod
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: weblate
    environment: prod
  type: LoadBalancer
