linters:
  flake8:
    fixer: true
  csslint: {  }
  shellcheck:
    shell: bash
  phpcs:
    standard: PSR2
    fixer: true
files:
  ignore:
    - 'bower_components/*'
    - 'node_modules/*'
    - 'vendor/*'
fixers:
  enable: true
