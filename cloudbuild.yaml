steps:
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
      - --destination=gcr.io/$PROJECT_ID/weblate:$BUILD_ID
      - --cache=true
      - --dockerfile=./mscli-Dockerfile
images: ['gcr.io/$PROJECT_ID/weblate:$BUILD_ID']
tags:
  - Weblate
timeout: 1200s
options:
  machineType: N1_HIGHCPU_8
